# URL de l'API backend — À configurer dans Vercel Dashboard > Environment Variables
# En local: laisser vide (proxied vers /api par défaut)
# En production: https://ton-backend.vercel.app/api
VITE_API_URL=
